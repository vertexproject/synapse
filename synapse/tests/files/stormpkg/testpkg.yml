name: testpkg
version: 0.0.1

modules:
    - name: testmod

external_modules:
    - name: testext
      path: stormmod/common
      package: synapse.tests.files

commands:
    - name: testcmd

optic:
    title: woot

    actions:

        - name: hehe
          storm: dofoo

        - name: barbar
          callback: dobar
    
