if (true) {
} else {
    $x = (1)
}
// lines: 1, 3
// coverage: 1
// arcs: (-1,1), (1,3), (1,-1), (3,-1)
// arcs_hit: (-1,1), (1,-1)

// NOTE: This is a tricky one because the if body is empty so even though the
// condition is true, the arc jumps directly to the exit (1, -1). Also, the else
// line is not considered a line of execution so there's no arcs to/from line 2.
