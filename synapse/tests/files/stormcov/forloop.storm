for $x in (foo, bar) {
    [ inet:fqdn=$x ]
}
// lines: 1, 2
// coverage: 1, 2
// arcs: (-1,1), (1,-1), (1,2), (2,-1), (2,2)
// arcs_hit: (-1,1), (1,2), (2,2), (2,-1)

// NOTE: There's no back edge (2, 1) on for loops because (unlike while loops)
// the condition (iteration in this case) is only evaluated once
