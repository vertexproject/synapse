---
desc: Added ``gate`` key support to the ``POST /api/v1/auth/user/<id>`` and ``POST /api/v1/auth/role/<id>``
  HTTP APIs, allowing ``rules`` and ``admin`` to be scoped to a specific auth gate.
desc:literal: false
prs: []
type: feat
...
