---
desc: Add support for a $salt key in GUID constructor dictionaries. The salt value
  is included in the GUID computation, allowing callers to produce distinct stable
  GUIDs from the same set of deconfliction properties.
desc:literal: false
prs: []
type: feat
...
