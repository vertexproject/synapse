---
desc: Added ``ipv4-addr``, ``ipv6-addr``, ``location``, ``file``, ``url``, and ``domain-name`` to the default STIX types supported by ``$lib.stix.import.ingest()``.
desc:literal: false
prs: []
type: feat
...
