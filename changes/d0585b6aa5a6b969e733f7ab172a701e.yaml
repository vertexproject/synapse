---
desc: Updated the pinned version of the ``aiohttp`` library to ``aiohttp>=3.14.1,<3.15.0``.
  Updated the pinned version of the ``aiohttp-socks`` library to ``aiohttp-socks>=0.11.0,<0.12.0``.
  Updated the pinned version of the ``vcrpy`` library to ``vcrpy>=8.3.0,<9.0.0``.
  Updated the pinned version of the ``aiosmtplib`` library to ``aiosmtplib>=5.1.2,<6.0.0``.
desc:literal: false
prs: []
type: note
...
