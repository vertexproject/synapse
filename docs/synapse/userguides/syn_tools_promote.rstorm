.. highlight:: none

.. _syn-tools-promote:

promote
=======

The Synapse ``promote`` tool can be used to promote a mirror to the leader.

Syntax
------

``promote`` is executed using ``python -m synapse.tools.promote``. The command usage is as follows:

.. shell:: --fail-ok python3 -m synapse.tools.promote -h
