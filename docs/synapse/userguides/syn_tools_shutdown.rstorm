.. highlight:: none

.. _syn-tools-shutdown:

shutdown
========

The Synapse ``shutdown`` tool can be used to initiate a graceful shutdown of a Synapse service.

Syntax
------

``shutdown`` is executed using ``python -m synapse.tools.shutdown``. The command usage is as follows:

.. shell:: --fail-ok python3 -m synapse.tools.shutdown -h
