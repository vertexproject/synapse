.. highlight:: none

.. _syn-tools-cellauth:

cellauth
========

The Synapse ``cellauth`` tool can be used to manage permissions of users, roles, and objects in a remote cell.

Syntax
------

``cellauth`` is executed using ``python -m synapse.tools.cellauth``. The command usage is as follows:

.. shell:: python3 -m synapse.tools.cellauth -h
